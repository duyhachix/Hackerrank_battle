/*
Phần từ lớn hơn tiếp theo của element X nào đó trong 1 array là phần tử lớn hơn đầu tiên, cái mà phía bên phải của X bên trong array.
Bạn được cung cấp hai mảng số nguyên 0-indexed riêng biệt là nums1 và nums2, trong đó nums1 là một tập hợp con của nums2.
Với mỗi 0 <= i <nums1.length, hãy tìm chỉ số j sao cho nums1 [i] == nums2 [j] và xác định phần tử lớn hơn tiếp theo của nums2 [j] trong nums2. 
Nếu không có phần tử lớn hơn tiếp theo, thì câu trả lời cho truy vấn này là -1

==> Trả về một mảng 'ans' có độ dài nums1.length sao cho ans[i] là phần tử lớn hơn tiếp theo như được mô tả ở trên.

Example 1:

Input: nums1 = [4,1,2], nums2 = [1,3,4,2]
Output: [-1,3,-1]
Explanation: The next greater element for each value of nums1 is as follows:
- 4 is star in nums2 = [1,3,4*,2]. There is no next greater element, so the answer is -1.
- 1 is star in nums2 = [1*,3,4,2]. The next greater element is 3.
- 2 is star in nums2 = [1,3,4,2*]. There is no next greater element, so the answer is -1.

*/

/**
 * @param {number[]} nums1 nums1 array
 * @param {number[]} nums2 nums2 array
 * @return {number[]} ans
 */
var nextGreaterElement = function (nums1, nums2) {};
// un-done
